apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Dev overlay: minimal footprint for a laptop / kind cluster.
resources:
  - ../../base

# Single replica per stateless tier — enough to develop against.
replicas:
  - name: api
    count: 1
  - name: nginx
    count: 1
